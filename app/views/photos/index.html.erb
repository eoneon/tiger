<div class="row mt-5">
  <div class="col-2">
    <%= render 'search_photos/aside' %>
  </div>

  <div class="col-9 bg-dark">
    <div class="row row-cols-1 row-cols-md-5 g-4 mt-5 bg-dark" id="photos-index">
      <%= render 'photos/index', photos: @photos %>
    </div>
  </div>
</div>
