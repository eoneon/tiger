<!DOCTYPE html>
<html>
  <head>
    <title>Josh Exotic</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.6/cropper.js"></script>
    <%= tag.meta name: "direct-upload-url", content: rails_direct_uploads_path %>
    <link href="https://releases.transloadit.com/uppy/v2.1.0/uppy.min.css" rel="stylesheet">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-danger">
    <div class="container">
    <%= render "search_photos/form" %>

      <nav class="navbar navbar-expand-lg bg-dark fixed-top mb-5">
        <div class="container-fluid">
          <a class="navbar-brand sexy_text text-warning fs-1" href="#">Josh Exotic's Prviate Parts</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="d-flex">
            <div class="collapse navbar-collapse d-flex">
              <ul class="navbar-nav">
                <li class="nav-item">
                  <a href="<%= photos_path %>" class="nav-link text-warning">
                    Photos
                  </a>
                </li>
                <li class="nav-item">
                  <a href="<%= categories_path %>" class="nav-link text-warning">
                    Categories
                  </a>
                </li>
                <li class="nav-item">
                  <a href="<%= new_photo_path %>" class="nav-link text-warning">
                    Add New Photo
                  </a>
                </li>
                <li class="nav-item">
                  <a href="<%= new_category_path %>" class="nav-link text-warning">
                    Add Categories
                  </a>
                </li>
              </ul>
            </div>
          </div>

        </div>
      </nav>

      <br>
      <br>
      <br>
      <br>

      <%= yield %>
    </div>

    <script src="https://releases.transloadit.com/uppy/v2.1.0/uppy.min.js"></script>
  </body>
</html>
